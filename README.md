# 🎭 Автотесты для MTS Live

Этот проект содержит автотесты для сайта [live.mts.ru](https://live.mts.ru) - платформы для покупки билетов на мероприятия.

## 🚀 Что тестируем

- Главная страница и навигация
- Карточки мероприятий (концерты, спектакли, стендап)
- Поиск и фильтрация событий
- Процесс бронирования билетов
- Авторизация пользователей
- API endpoints

## 🛠 Технологии

- **TypeScript** - типизация и надежность
- **Playwright** - современный фреймворк для тестирования
- **Page Object Model** - удобная структура тестов
- **Allure Reports** - детальные, красивые отчеты
- **GitHub Actions** - автоматический запуск тестов CI/CD

## 📁 Структура проекта

```bash
src/
├── pages/ # Страницы сайта
├── components/ # Компоненты (хедер, карточки, футер)
├── constants/ # Константы и селекторы
├── types/ # Типы TypeScript
└── utils/ # Вспомогательные функции

tests/
├── smoke/ # Быстрые проверки
├── features/ # Функциональные тесты
└── api/ # API тесты
```

## 🐳 Docker & Makefile автоматизация

### Основные команды Makefile

```bash
make build           # Собрать Docker образ
make test           # Запустить все тесты
make test-smoke     # Только smoke тесты
make test-features  # Только функциональные тесты
make test-api       # Только API тесты
make test-ui        # Запуск в UI режиме (отладка)
make test-headed    # Тесты с видимым браузером
make allure-report  # Сгенерировать Allure отчет
make allure-serve   # Запустить сервер с отчетами
make clean          # Очистить отчеты и кэш
make full           # Полный цикл: тесты → отчеты → сервер
```

### Разработка с мониторингом

```bash
# Режим живого мониторинга (тесты перезапускаются при изменениях)
make test-watch
```

### Бенчмаркинг производительности

```bash
# Замерить производительность тестов
make benchmark
# Результаты сохраняются в test-results/benchmark-*.json
```

## 🏃‍♂️ Быстрый старт

### 1. Установка

```bash
# Клонируем проект
git clone [your-repo-url]
cd live_mts_ru

# Устанавливаем зависимости
npm install

# Устанавливаем браузеры для тестов
npx playwright install
```

### 2. Запуск тестов

```bash
# Все тесты
npx playwright test

# Только быстрые проверки
npx playwright test tests/smoke/

# С визуальным интерфейсом
npx playwright test --ui

# Тесты в браузере
npx playwright test --headed

# Тесты в определенном браузере
npx playwright test --project=chromium

# Тесты с дебагом
npx playwright test --debug
```

### 3. Просмотр отчетов

```bash
# HTML отчет Playwright
npx playwright show-report

# Генерация Allure отчета
allure generate allure-results --clean

# Просмотр Allure отчета
allure serve allure-results
```

## 🧪 Виды тестов

### Smoke тесты

- Проверка загрузки главной страницы
- Наличие основных элементов
- Работоспособность навигации

### Функциональные тесты

- Карточки мероприятий
- Поиск событий
- Процесс бронирования
- Авторизация

### API тесты

- Авторизация через API
- Получение списка мероприятий
- Поиск событий

### 🔧 Настройка

#### Локальная настройка

Создайте файл .env:

```env
BASE_URL=<https://live.mts.ru>
TEST_USER_PHONE=+79991112233
TEST_USER_PASSWORD=your_password
HEADLESS=true
SLOW_MO=0
```

### Pre-commit проверки

Перед каждым коммитом автоматически запускаются:

✅ Проверка компиляции TypeScript

✅ Быстрые smoke тесты

✅ Проверка стиля кода (если настроено)

### Запуск в Docker

```bash
docker-compose up --build
```

## 📊 Отчеты

После запуска тестов доступны:

### Allure отчеты

```bash
make allure-serve
# Отчет доступен на http://localhost:8050
```

### Playwright отчеты

```bash
make serve-playwright  
# Отчет доступен на http://localhost:8080
```

## 🤝 Для разработчиков

### Добавление нового теста

1. Создайте Page Object в `src/pages/`
2. Добавьте селекторы в `src/constants/Selectors.ts`
3. Напишите тест в соответствующей папке `tests/`

### Пример теста

```typescript
test('проверка карточек мероприятий', async ({ page }) => {
  const homePage = new HomePage(page);
  await homePage.open();
  
  const cardsCount = await homePage.eventCards.getCardsCount();
  expect(cardsCount).toBeGreaterThan(0);
});
```

## 📈 CI/CD

### GitHub Actions

Тесты автоматически запускаются:

- При push в `main` или `dev` ветки
- При создании Pull Request
- Вручную через GitHub Interface

### Ручной запуск в GitHub

1. Перейдите в **Actions** → **Playwright Tests with Allure Reports**
2. Нажмите **Run workflow**
3. Выберите тип тестов (smoke, features, api, all)

Отчеты публикуются на GitHub Pages:  
https://ваш-username.github.io/ваш-репозиторий/

### 🔧 Настройка GitHub Pages

1. Перейдите в **Settings → Pages**
2. **Source**: GitHub Actions
3. Сохраните настройки

## 🪲 Поиск проблем

Если тесты не работают:

1. Проверьте подключение к интернету
2. Убедитесь, что сайт live.mts.ru доступен
3. Запустите с --headed для визуальной отладки
4. Проверьте логи в отчетах Allure

## 📞 Поддержка

- Баг-репорты: создавайте issue в репозитории
- Вопросы: проверьте документацию Playwright
- Предложения: welcome! 🎉
