# ⚠️ Известные предупреждения проекта

> [!IMPORTANT]
> Этот файл содержит актуальные предупреждения и проблемы конкретного проекта MTS Live Autotests.

## 🚨 Активные предупреждения

### 1. Playwright Update Available

**Статус:** 🔶 ТРЕБУЕТ ОБНОВЛЕНИЯ  
**Приоритет:** Средний

**Сообщение:**

```bash
A new version of Playwright is available: 1.40.0 → 1.41.0
```

Файлы: `package.json`  

Решение:

```bash
npm update playwright
npx playwright install
```

### 2. Deprecated Page Methods

**Статус**: 🔶 ТРЕБУЕТ ИСПРАВЛЕНИЯ  
**Приоритет**: Высокий

Сообщение:

```bash
page.$() is deprecated - use page.locator() instead
```

Затронутые файлы:

- `src/pages/HomePage.ts` (строки 45, 78)

- `src/pages/EventPage.ts` (строки 23, 56)

- `src/components/Header.ts` (строка 34)

Исправление:

```typescript
// БЫЛО
await page.$('.selector');

// СТАЛО
await page.locator('.selector').click();
```

### 3. Configuration Warnings

**Статус**: 🔶 ТРЕБУЕТ ИСПРАВЛЕНИЯ  
**Приоритет**: Средний

Сообщение:

```bash
Use 'testDir' instead of deprecated configuration option
```

Файл: `playwright.config.ts` (строка 15)  

Решение:

```typescript
// БЫЛО
projects: [
  {
    use: { ... }
  }
]

// СТАЛО
testDir: './tests',
projects: [
  {
    name: 'chromium',
    use: { ... }
  }
]
```

### 4. TypeScript Strict Mode

**Статус**: 🔶 ТРЕБУЕТ ИСПРАВЛЕНИЯ
**Приоритет**: Низкий

Сообщение:

```bash
Object is possibly 'null'
```

Затронутые файлы:

- `src/utils/helpers.ts` (строки 12, 45)

- `src/types/api.ts` (строка 78)

Решение:

```typescript
// Добавить проверки
if (await element.isVisible()) {
  await element.click();
}
```

## 📊 Прогресс исправления

|Ворнинг|Статус|Назначено|Дедлайн|
|----|----|---|---|
|Deprecated Methods|🟡 В работе|@user|2024-01-22|
|Playwright Update|🟢 Запланировано|@user|2024-01-29|
|Configuration|🟢 Запланировано|@user|2024-01-29|
|TypeScript|🔴Отложено|-|2024-02-15|

## 🛠️ Команды для проверки

```bash
# Проверить TypeScript
npx tsc --noEmit

# Проверить обновления
npx playwright install --dry-run

# Запустить тесты с детальным выводом
npx playwright test --verbose tests/smoke/

# Проверить неиспользуемые зависимости
npm depcheck
```

### 📈 Статистика

Последнее обновление: 2024-01-15  
Всего ворнингов: 8  
Исправлено: 2  
В работе: 3  
Осталось: 3  
Уровень качества: 75%  

>[!NOTE]
>Политика проекта:
>
>- Критические ворнинги → 24 часа
>
>- Важные ворнинги → 1 неделя
>
>- Незначительные → 1 месяц
>
>- Все изменения проверяются через smoke тесты
