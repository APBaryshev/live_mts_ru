# 🧪 Руководство по тестированию

## Виды тестов

### Smoke тесты

**Назначение**: Быстрая проверка работоспособности основных функций  
**Время выполнения**: ~1-2 минуты

```bash
npx playwright test tests/smoke/
```

Проверяет:

✅ Загрузка главной страницы

✅ Наличие основных элементов интерфейса

✅ Работоспособность навигации

✅ Доступность карточек мероприятий

## Функциональные тесты

**Назначение**: Полная проверка функциональности  
**Время выполнения**: ~5-10 минут

```bash
npx playwright test tests/features/
```

Проверяет:

✅ Поиск и фильтрация мероприятий

✅ Работа с карточками событий

✅ Процесс бронирования билетов

✅ Авторизация пользователя

✅ Корзина и оформление заказа

## API тесты

**Назначение**: Проверка API endpoints  
**Время выполнения**: ~2-3 минуты

```bash
npx playwright test tests/api/
```

Проверяет:

✅ Авторизация через API

✅ Получение списка мероприятий

✅ Поиск событий через API

✅ Работа с корзиной через API

## Запуск тестов

### Локальный запуск

```bash
# Все тесты
make test

# Конкретный тип тестов
make test-smoke
make test-features  
make test-api

# Тесты с видимым браузером
make test-headed

# Режим отладки
make test-ui
```

### Параметры запуска

```bash
# Запуск в определенном браузере
npx playwright test --project=chromium
npx playwright test --project=firefox
npx playwright test --project=webkit

# Параллельный запуск
npx playwright test --workers=4

# Запуск с таймаутом
npx playwright test --timeout=60000
```

## Отчеты

### Allure отчеты

```bash
# Генерация отчета
make allure-report

# Просмотр отчета
make allure-serve
```

### Playwright отчеты

```bash
# HTML отчет
npx playwright show-report
```

>[!IMPORTANT]
>Рекомендации по тестированию:
>
>- Запускайте smoke тесты перед каждым коммитом
>
>- Полный прогон делайте перед мержем в main
>
>- Используйте тестовые данные аккуратно
>
>- Следите за лимитами запросов к API
